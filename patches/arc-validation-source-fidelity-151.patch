diff --git a/minimax.py b/minimax.py
--- a/minimax.py
+++ b/minimax.py
@@
         {
             "role": "system",
             "content": (
-                "Validate one story arc against its source. Report only the first "
-                "real blocking semantic issue. Return JSON only."
+                "Check only whether the proposed chronological event list covers "
+                "the explicit source actions/states without source-order "
+                "contradiction or an unsupported major plot addition. Report only "
+                "the first real blocking issue. Do not critique pacing, bundling, "
+                "detail level, style, or beat allocation. Return JSON only."
             ),
         },
@@
 - DEFINED SUBJECTS establish identity aliases. Relational wording such as
   "Amy's kids" may refer to named defined Subjects without repeating their
   names in every event.
+- Do not require clothing, appearance, names, or other descriptive facts that
+  the source does not provide. Validate source fidelity; do not invent missing
+  source details as requirements.
 - Preserve an explicit ordinary baseline as its own beat before a sudden
   inciting threat/change when the beat budget permits.
@@
-- Adjacent source actions may share one required_event when needed; do not join
-  distant story stages or drop a source action.
+- Adjacent sequential source actions may share one required_event when needed.
+  This is not an error when their source order is preserved inside the event;
+  do not join distant story stages or drop a source action.
@@
-- A defined human Subject must have concrete clothing when first shown.
 - state_effects must describe only persistent facts directly established by the
   owning event. Reject inferred/temporary conditions and wrong typed operations.

diff --git a/docs/HANDOFF.md b/docs/HANDOFF.md
--- a/docs/HANDOFF.md
+++ b/docs/HANDOFF.md
@@
 Next evidence boundary: a fresh planning-only locked Amy acceptance. Do not add
 the previously probed safe-room/front-handoff repair unless that acceptance
 proves it is the earliest remaining failure.
+
+## Planning-only acceptance 147: broad ARC validator still over-judges source fidelity
+
+`run-acceptance-amy-flat-arc-planning-147` reached broad ARC VALIDATE with a
+structurally clean flat eight-event plan, but validation again failed on facts
+that were already present or were never required by the source. The earliest
+false rejection demanded clothing for Will/Amber even though the source never
+specifies it. Later retries falsely claimed that the kids' identities or the
+breakfast action were absent despite relational wording and the explicit Beat-1
+event.
+
+Targeted probes narrowed the validator contract further:
+
+- `arc-validation-source-clothing-probe-148` removed the unsupported clothing
+  requirement; the same model then falsely claimed the explicit weapon-equip
+  event was missing.
+- `arc-validation-minimal-coverage-probe-149` reduced the task to source
+  coverage/order and then incorrectly criticized legal bundling of adjacent
+  sequential source actions.
+- `arc-validation-minimal-bundling-probe-150` explicitly limited broad
+  validation to missing source actions/states, source-order contradictions, or
+  unsupported major plot additions and stated that adjacent bundling is legal.
+  The same candidate validated cleanly.
+
+The evidence says broad ARC VALIDATE should be a narrow source-fidelity check,
+not a place to invent descriptive requirements or critique pacing/allocation.
+Majority allocation remains owned by the existing focused majority check.
+
+The focused production patch is staged as
+`patches/arc-validation-source-fidelity-151.patch`. It removes the generic
+"every defined human needs clothing" requirement from broad ARC validation,
+forbids requirements for source-absent descriptive facts, explicitly permits
+order-preserving adjacent bundling, and narrows the system instruction to the
+three actual broad-fidelity failures. This preserves ARC CREATE -> VALIDATE ->
+REPAIR and adds no semantic layer.
+
+After applying 151, run focused prompt regressions and then another planning-only
+locked Amy acceptance. Do not implement the safe-room/front-handoff repair until
+that run proves it is the earliest remaining failure.
