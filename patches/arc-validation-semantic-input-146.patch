diff --git a/minimax.py b/minimax.py
--- a/minimax.py
+++ b/minimax.py
@@
 def _format_beat_arc_subject_names(subject_info: str) -> str:
     """Return compact subject identity lines for beat/arc semantic planning."""
 
     lines: list[str] = []
     for raw_line in (subject_info or "").splitlines():
-        subject_name = _subject_name_from_line(raw_line)
-        if subject_name and subject_name not in lines:
-            lines.append(subject_name)
+        subject_id = _subject_name_from_line(raw_line)
+        if not subject_id:
+            continue
+        match = re.match(
+            r"^\s*(<Subject\s+\d+>)\s+is\s+([^,.]+)",
+            raw_line,
+            flags=re.IGNORECASE,
+        )
+        compact = (
+            f"{match.group(1)} = {match.group(2).strip()}"
+            if match
+            else subject_id
+        )
+        if compact not in lines:
+            lines.append(compact)
     return "\n".join(lines)
@@
 def build_macro_arc_validation_messages(
@@
-    arc_json = json.dumps(macro_arc, ensure_ascii=False, indent=2)
+    # ARC semantic validation should judge the LLM-authored beat jobs, not the
+    # deterministic one-beat phase wrappers Python adds for downstream code.
+    validation_events = []
+    for phase in macro_arc.get("phases", []):
+        validation_events.extend(phase.get("required_events", []))
+    arc_json = json.dumps(
+        {"events": validation_events},
+        ensure_ascii=False,
+        indent=2,
+    )
@@
                 "- SOURCE COVERAGE FIRST: every explicit visible source action/state must appear "
                 "in required_events in the same order. Calm/mundane setup still counts.\n"
+                "- DEFINED SUBJECTS establish identity aliases. Relational wording such as "
+                "'Amy's kids' may refer to named defined subjects without repeating their names.\n"
+                "- When the source explicitly describes an extended or repeated process, such as "
+                "a sequence occupying the majority of the story, multiple beats may continue that "
+                "same source-authorized process. Do not reject those continuations merely because "
+                "the source states the repeated process once. Reject only a new major plot, "
+                "character, location, mechanism, or outcome not authorized by the source.\n"
+                "- Judge only required_events and state_effects in PROPOSED ARC. Python-owned "
+                "phase bookkeeping is intentionally not part of semantic validation.\n"
                 "- Do NOT judge majority/relative-duration allocation in this broad check; a "
